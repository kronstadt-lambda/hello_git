export const git = 'git'

/*
 * (currentbranch) git diff <branch>: shows the differences between the current branch and the branch.
 * (dev branch) git merge <branch>: merges the branch to the current branch dev.
 */