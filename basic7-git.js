export function git7() {
    console.log('This is a basic7 git');
}
/*
 * Temporary commits
 * git stash: save modified and staged changes (both from staging area and working directory).
 *           Note: New untracked files are ignored by stash.
 * git stash list: list stack-order of stashed file changes.
 * git stash pop: write working from top of stash stack.
 * git stash drop: discard the changes from top of stash stack.
 *
 */