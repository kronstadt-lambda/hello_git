export function basic3Git() {
    console.log('This is a basic3 git');
}
/*
 * git tag <name>: creates a tag with the name. It is useful to mark a specific task end or version.
 * git tag: show all tags.
 * git checkout tags/<tag name>: this command changes to the tag.
 * git revert <hash>: this deletes the commit with the hash and creates a new commit with the changes. It is useful with bugs exist.
 */

